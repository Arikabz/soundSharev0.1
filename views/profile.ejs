<%- include('partials/header') -%>
<div id='wrapper'>
<div class="main">
<div class='inner'>
<div class='row'>
    <div class='col-2'>
        <span class='image circle fit'><img class="" src="<%=user.image%>"></span>
        <% if(user.id===currentUser.id) {%>
        <a href='/profilePictureUpdate' class='button small'>edit</a>
        <% } %>
    <p><strong><%= user.userName %></strong></p>
        <% if(user.id===currentUser.id) {%>
    <p><strong>Email</strong>: <%= user.email %></p>
        <% } %>
    <a href="/logout" class="col-3 button next">Logout</a>
    <a href="/upload" class="col-3 button next">Upload</a>
    </div>
    <section class='col-9 row off-1'>
    <div class='box alt'>
      <div class="row gtr-uniform">
        <% for(var i=0; i<audios.length; i++) {%>
        <div class='col-12 row gtr-uniform'>
            <div class='col-3'>
                <a href="/post/<%= audios[i]._id%>" class=''>
                    <span class='image square fit'>
                        <% if (audios[i].customImg){ %>
                            <img class="" src="<%= audios[i].customImg%>">
                        <% } else { %>
                            <img class="" src="<%= audios[i].defaultImg%>">
                        <% }%>
                    </span>
                </a>
            </div>
            <div class='col-8 off-1'>
                <h4><%=audios[i].title%>
                <a class='icon brands' href='<%=audios[i].audio%>'>
                    <i class='fas fa-download'></i>
                </a>
                </h4>
                <figure>
                <audio
                    controls
                    src='<%=audios[i].audio%>'>
                </audio>
                </figure>
            </div>
        </div>
        <% } %>
      </div>
    </div>
    </section>
</div>
        <div class="row aln-center">
          <a class="button next" href="/feed">Return to Feed</a>
        </div>  
</div>
</div>
<%- include('partials/footer') -%>
